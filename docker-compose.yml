version: '3.8'

services:
  currency-app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # Пробрасываем папку с данными, чтобы отчёты сохранялись на хосте
      - ./data:/app/data
    restart: unless-stopped
